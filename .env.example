# Database (points to Docker Compose services)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=nestjs_db

# Redis (points to Docker Compose services)
REDIS_HOST=localhost
REDIS_PORT=6379

# App
PORT=3000
